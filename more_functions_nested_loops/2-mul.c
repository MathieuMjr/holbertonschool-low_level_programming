#include "main.h"

/**
 * mul - mutliplies 2 int
 * @a : one int to multiply with another
 * @b : another int to multiply with the first one
 (*
 * Description: Longer description of the function)?
(* section header: Section description)*
 * Return: result of a *b
 */
int mul(int a, int b)
{
	int i;

	i = a * b;
	return (i);
}
