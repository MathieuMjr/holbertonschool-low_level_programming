#include "main.h"

/**
 * add - makes an add with 2 ints
 * @a : one number you want to add to another
 * @b : another number tyou want to add to the 1st one
 (*
 * Description: Longer description of the function)?
(* section header: Section description)*
 * Return: result of operation
 */
int add(int a, int b)
{
	return (a + b);
}
